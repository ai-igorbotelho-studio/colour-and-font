# Cloudflare Pages — configuração lida pelo build do Pages (Git integration).
# Comando de build no painel: npm run build · saída: dist · Node: .node-version
name = "auge"
pages_build_output_dir = "dist"
compatibility_date = "2026-09-01"
